'use strict';

define(['angular'], function(angular) {
  angular.module('<%= _.camelize(appName) %>').controller('<%= _.classify(name) %>Ctrl', function ($scope) {
    // TODO: not implemented yet
  });
});
